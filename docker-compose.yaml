services:
  postgres:
    image: postgres:16
    container_name: ea-postgres
    environment:
      POSTGRES_DB: elastic_autograder
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: ea-redis
    ports:
      - "6379:6379"

volumes:
  pgdata: