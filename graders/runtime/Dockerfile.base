FROM python:3.11-slim

WORKDIR /app

ARG RUNTIME_PACKAGES=""

RUN if [ -n "$RUNTIME_PACKAGES" ]; then \
      apt-get update \
      && apt-get install -y --no-install-recommends $RUNTIME_PACKAGES \
      && rm -rf /var/lib/apt/lists/*; \
    fi

COPY runtime/main.py /app/main.py
